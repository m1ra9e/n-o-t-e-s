# monolith with jre (not use Jigsaw + JLink)

An example of assembling an application that contains JRE (that is, the assembled application does not require Java to be installed on the client PC).

# Build

```sh
mvn clean package
```
